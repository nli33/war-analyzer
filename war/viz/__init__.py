"""Static visualization outputs: PLAN.md Section 6 scatter plots and tables.

Rendered with matplotlib's non-interactive `Agg` backend so this runs in the
CLI-only overnight sandbox (no display/browser) — see SCOPE.md Phase 6 and
OVERNIGHT.md.
"""
